
; how about stream-map a**2 + b**2, and filter if integers? this'll pick out right triangles.

; also test against built-in implementation

; is there any way to confirm that it's not running really really far? maybe with the mapped function also calling display?
; and third, compare with non-stream map