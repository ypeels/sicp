(load "2.56-58-symbolic-differentiation.scm")


; TODO: simplify all numbers?
(define (make-sum-2.57 . args)

    ; no, i think i'm overthinking this... should just be a QUICK PATCH to addend, augend, multiplicand...
    ;(if (< (length args) 2)
    ;    (error "too few arguments: make-sum-2.57" (length args))
    ;    (let 

    


;(define 